/*
 * Class Divide, public not private.
 */
package numericalcalculator;

/**
 * Divide 2 parameters, num1 and num2, and store the result into the variable 
 * 'result'. All 3 variables have are of type double. Method's name is division. 
 * @author poclitar
 */
public class Divide {
    public double division (double num1, double num2){
        double result = (num1 / num2);
        return (result);
    }
    
}
