/*
 * Class Equal.
 */
package numericalcalculator;

/**
 * Display the ops result using variable memoDisplay type double. 
 * @author poclitar
 */
public class Equal { // class named Equal
    public void display (double  memoDisplay){ /* void doesn't return a result, 
        * method's name is display, the variable's name is memoDisplay and the 
        * type is double */
        System.out.println(memoDisplay);
    }
    
}
